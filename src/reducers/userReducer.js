const initialState = {
    name: 'anthony',
    other: []
}

export default function userDataReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}